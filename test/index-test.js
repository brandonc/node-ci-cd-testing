const assert = require("assert");

// This is a comment
// and another comment
describe("Index", () => {
  it("should pass", () => {
    assert.equal(1, 1);
  });
  it("should be true", () => {
    assert.equal(true, true);
  });
});
